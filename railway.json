{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "gunicorn gram_predictor.app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120",
    "restartPolicyType": "always"
  }
}
