# 生产环境依赖文件
# 基于原有 requirements.txt，添加生产环境特定依赖

# 原有依赖
numpy>=1.20.0
torch>=1.9.0
transformers>=4.20.0
geoopt>=0.5.0
matplotlib>=3.4.0
seaborn>=0.11.0
scikit-learn>=0.24.0
pandas>=1.3.0
tqdm>=4.60.0
tensorboard>=2.8.0
Flask>=2.0
biopython>=1.79
peptides>=0.3.0
tensorflow>=2.8.0

# 生产环境依赖
gunicorn>=20.1.0
gevent>=21.12.0  # 可选的异步工作进程
psutil>=5.8.0    # 系统监控
redis>=4.0.0     # 缓存 (可选)

# 监控和日志
sentry-sdk>=1.5.0  # 错误监控 (可选)

# 安全
cryptography>=3.4.8

# 性能优化
ujson>=5.1.0     # 更快的 JSON 解析
