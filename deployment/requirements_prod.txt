# 超轻量级生产环境依赖 - 专门解决磁盘空间问题
# 移除了所有可能导致大型依赖的包

# 核心 Web 框架（精确版本避免意外依赖）
Flask==3.1.1
gunicorn==20.1.0
werkzeug==3.1.3
jinja2==3.1.6
markupsafe==3.0.2

# 最小数据处理库
numpy==1.24.3
pandas==2.0.3

# 生物信息学（最新稳定版）
biopython==1.85

# 基础依赖
requests==2.31.0
click==8.1.7
