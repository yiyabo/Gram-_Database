# 轻量级生产环境依赖文件
# 移除重型 ML 库以解决磁盘空间问题

# 核心 Web 框架
Flask>=2.0
gunicorn>=20.0.0
werkzeug>=2.0.0
jinja2>=3.0.0
markupsafe>=2.0.0

# 数据处理（基础版本）
numpy>=1.20.0
pandas>=1.3.0
scikit-learn>=0.24.0

# 生物信息学库
biopython>=1.79
peptides>=0.3.0

# 基础依赖
requests>=2.25.0
click>=7.0

# 生产环境特定依赖
gunicorn>=20.1.0
gevent>=21.12.0  # 可选的异步工作进程
psutil>=5.8.0    # 系统监控
redis>=4.0.0     # 缓存 (可选)

# 监控和日志
sentry-sdk>=1.5.0  # 错误监控 (可选)

# 安全
cryptography>=3.4.8

# 性能优化
ujson>=5.1.0     # 更快的 JSON 解析
